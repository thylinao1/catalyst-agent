# Catalyst Agent — runtime dependencies
# The LLM API is called over plain REST; storage uses the stdlib sqlite3.
requests>=2.31.0
python-dotenv>=1.0.0
feedparser>=6.0.0

# Dashboard only (streamlit run dashboard.py)
streamlit>=1.30.0
pandas>=2.0.0
